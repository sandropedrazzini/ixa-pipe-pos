# This is an example logger configuration, to show what to
# replace / update at runtime
# Documentation: https://logging.apache.org/log4j/2.x/manual/configuration.html
# To overwrite the standard configuration, you need to provide one of following files
# at root level: log4j2.properties, log4j2.yml, log4j2.json, or log4j2.xml

appender.stdout.type = Console
appender.stdout.name = stdout
logger.app = DEBUG, stdout
logger.app.name = eus.ixa.ixa